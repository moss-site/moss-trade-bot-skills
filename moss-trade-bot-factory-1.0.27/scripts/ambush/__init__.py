"""Ambush (异动币) strategy scripts — chained-harness backtest, propose, upload,
live_runner, and supporting modules.

Mirrors the layout of ``scripts/core/``: an explicit empty ``__init__.py`` so
the package is a regular Python package, even though Python 3.3+ implicit
namespace packages would also work.
"""
